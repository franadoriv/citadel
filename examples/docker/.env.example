# Copy to .env and set a long, unique value before starting Compose.
CITADEL_CONSOLE_PASSWORD=replace-with-a-long-unique-password

# The default Compose image is `citadel:local`; build it from the checkout first.
# CITADEL_IMAGE=citadel:local
