# Citadel standalone server configuration.
#
# This is the drop-and-run default: place this file next to `citadel(.exe)` and
# start the server with no arguments. On first run the node creates its SQLite
# database file and an empty `game/` folder, applies migrations, and starts
# listening. Edit the values below and restart to change behavior (the `game/`
# scripts hot-reload without a restart when `runtime.hot_reload = true`).
#
# Every key is optional and falls back to a built-in default; delete a section to
# accept the defaults. Pass `--config <path>` to load a different file, or set
# `CITADEL_*` environment variables to override individual values.

[server]
# Stable identifier for this node (shown on /status and in logs).
node_id = "citadel-1"
# Address other nodes/clients use to reach this node.
public_addr = "127.0.0.1:7350"

[http]
# HTTP surface: health (/health), status (/status), and the admin console
# (/dashboard). Bind to 0.0.0.0:7350 to accept connections from other machines.
# Binding a non-loopback address requires real console credentials below: the
# server refuses to start while the built-in operator password is still in use.
# It also requires encryption, since this listener carries the console password,
# the console bearer tokens, and every player session token. Either set
# [http.tls] below, or set behind_tls_proxy when a TLS-terminating reverse proxy
# fronts this listener. Loopback binds are exempt.
bind = "127.0.0.1:7350"
# behind_tls_proxy = true

# [http.tls]
# Terminate HTTPS directly on this listener, with no reverse proxy needed. The
# key may be PKCS#8, RSA/PKCS#1 or SEC1 PEM. ALPN advertises HTTP/2 and
# HTTP/1.1. This is independent of [transport.tls], which covers the QUIC and
# WebTransport listeners: those are usually issued a different certificate.
# certificate_file = "/etc/citadel/tls/fullchain.pem"
# private_key_file = "/etc/citadel/tls/privkey.pem"

# [console]
# Credentials for the operator console at /dashboard and its /console/v1/* API.
# The built-in default (admin/password) exists so the local demo runs with zero
# setup, and is accepted ONLY on a loopback [http] bind. Uncomment and set real
# values before exposing this node, or supply CITADEL_CONSOLE_USERNAME and
# CITADEL_CONSOLE_PASSWORD from your secret store instead of committing them.
# The console has no separate transport: it is served over plain HTTP, so put it
# behind a TLS-terminating reverse proxy or keep it on loopback.
# username = "operator"
# password = "choose-a-long-random-value"
# viewer_password = "optional-read-only-value"
# token_expiry_sec = 3600

[logging]
# Log verbosity directive (e.g. "info", "debug", "citadel=trace") and output
# format ("pretty" for humans, "json" for log aggregation).
level = "info"
format = "pretty"

[errors]
# Redacted server failures and panics are retained in citadel-errors.jsonl next
# to the server binary. Oldest entries are pruned when either bound is reached.
max_bytes = 8388608
max_entries = 2000

[telemetry.authoritative_decisions]
# Node-local, bounded records for validated authoritative decisions. Only opaque
# numeric correlations plus generic outcome/reason categories are retained; no
# payload, reply, command, corrected value, participant, or account data.
enabled = true
capacity = 1024

[telemetry.slices]
# Trusted runtime code may begin/mark/finish context-bound slices. Citadel closes
# slices at the TTL or configured caps and retains only bounded, redacted reports.
max_active = 32
max_markers = 32
ttl_ms = 300000
max_closed_reports = 128

[database]
# Persistence backend, selected by URL scheme:
#   sqlite://<file>            -> embedded single-file SQLite (created on first run)
#   postgres://user:pass@host/db -> PostgreSQL
#   mongodb://user:pass@host/citadel?replicaSet=rs0 -> MongoDB replica set
#   mongodb+srv://user:pass@cluster/citadel -> MongoDB SRV deployment
# Remove this key entirely to run non-durably on the in-memory backend.
# The standalone default is a single `data.sqlite` file next to the binary.
url = "sqlite://data.sqlite"
# MongoDB uses the official driver's URI TLS/authentication support (SCRAM and
# X.509). For the transactional contract keep these explicit majority/primary
# values; MongoDB must be a replica set or sharded cluster, never standalone.
mongodb_read_preference = "primary"
mongodb_write_concern = "majority"
mongodb_read_concern = "majority"
# Connection pool size and timeouts (milliseconds).
max_connections = 10
connect_timeout_ms = 5000
acquire_timeout_ms = 5000

# Optional operator-declared storage indexes. Uncomment and customize one entry
# per indexed storage collection; they are created safely at server startup.
# [[storage.indexes]]
# name = "profiles_by_score"
# collection = "profiles"
# fields = ["score", "region"]

[runtime]
# Embedded game-logic runtime. With language unset, Citadel autodetects one
# entrypoint in scripts_dir: main.lua (Lua, shipped), main.py (planned), or
# main.ts/main.js (planned). With no script present it falls back to the
# built-in relay.
enabled = true
# Optional explicit language. Omit for autodetect. Supported value today: "lua".
# language = "lua"
# Runtime hosting model. Only embedded/trusted is implemented today.
adapter = "embedded"
tier = "trusted"
# Folder holding your game scripts (created empty on first run). Drop a
# `main.lua` here to add Lua game logic. See website/src/content/docs/reference/server-sdk/lua-runtime.md.
scripts_dir = "./game"
# Per-invocation time budget for message/lifecycle handlers (milliseconds).
deadline_ms = 100
# Server game-loop rate in ticks per second (0 disables the `citadel.on_tick`
# loop; no periodic task is spawned).
tick_hz = 0
# Live-reload `game/main.lua` on save without a restart. Reloads are failure-safe
# (a broken edit is rejected and the previous script keeps serving). Convenient
# for development; consider turning it off for a fixed production deployment.
hot_reload = true
hot_reload_poll_ms = 500

# Authoritative gameplay bridge quotas + capabilities. Applied only when
# require_script is enabled (authoritative matches); ignored in the default
# relay mode. Quota defaults are PROVISIONAL (measure-first): they mirror the
# per-invocation command-sink precedents until the bench harness fixes them.
# Every capability defaults OFF (opt-in): a script may emit Persist/Schedule/
# physics commands in an authoritative match only when granted here. This is
# deployment-wide until the revision store declares capabilities per revision.
[runtime.bridge]
max_commands = 1024
max_command_body_bytes = 1048576
max_reply_bytes = 65536
max_recipients = 1024
max_persist_ops = 64
max_schedule_ops = 64
allow_persist = false
allow_schedule = false
allow_physics = false

# Runtime extension capabilities. Existing bounded outbound HTTP remains enabled
# for compatibility. Script-defined external endpoints are available but
# disabled by default. Events and the shared cache are node-local, non-durable,
# and opt-in.
# These are node-startup policy values: runtime script hot reload never changes
# them, so restart Citadel after editing this section.
[runtime.capabilities.outbound_http]
enabled = true
max_concurrent_requests = 16
max_requests_per_minute = 120
# Empty permits public DNS hosts; set exact names to further restrict egress.
allowed_hosts = []
# Public web ports only. Add an explicit private integration port if needed.
allowed_ports = [80, 443]
# Never enable this unless the listed host and private target are operator-owned.
allow_private_networks = false

[runtime.capabilities.custom_http_endpoints]
enabled = false
max_request_bytes = 65536
max_response_bytes = 1048576
max_requests_per_minute = 120

[runtime.capabilities.events]
enabled = false
queue_capacity = 1024
max_event_bytes = 16384
max_events_per_minute = 600

[runtime.capabilities.shared_cache]
enabled = false
max_entries = 1024
max_value_bytes = 65536
max_ttl_ms = 3600000

# Production receipt-validation foundation. Citadel owns provider egress and
# credentials; secrets are referenced only by environment-variable name, never
# placed in this file. Every real store remains disabled until its verified
# adapter ships. The custom development receipt validator remains available for
# local prototypes and tests.
[purchases]
max_receipt_bytes = 65536
max_concurrent_requests = 16
max_requests_per_minute = 120
timeout_ms = 5000
# Future adapters may retry only idempotent provider operations within this bound.
max_retries = 2
allowed_hosts = ["api.storekit.itunes.apple.com", "androidpublisher.googleapis.com"]

[purchases.apple]
enabled = false
# credential_env = "CITADEL_APPLE_PRIVATE_KEY"

[purchases.google]
enabled = false
# credential_env = "CITADEL_GOOGLE_SERVICE_ACCOUNT_JSON"

[purchases.huawei]
enabled = false
# credential_env = "CITADEL_HUAWEI_CLIENT_SECRET"

# Realtime transports. Each is independently enabled and bound. QUIC is the
# primary native transport, WebTransport serves browsers over HTTP/3, and
# WebSocket is the broadest-compatibility fallback. All three are enabled here so
# the standalone server accepts every supported client out of the box.
[transport.tls]
# Public QUIC/WebTransport TLS. Leave both paths commented for local development;
# Citadel then creates a short-lived self-signed certificate. For production set
# both to a CA-issued PEM chain and its private key (not used by WebSocket).
# certificate_file = "/etc/letsencrypt/live/game.example.com/fullchain.pem"
# private_key_file = "/etc/letsencrypt/live/game.example.com/privkey.pem"

[transport.quic]
enabled = true
bind = "127.0.0.1:7351"
outbound_queue_capacity = 1024

[transport.websocket]
enabled = true
bind = "127.0.0.1:7352"
outbound_queue_capacity = 1024

[transport.webtransport]
enabled = true
bind = "127.0.0.1:7353"
outbound_queue_capacity = 1024

# Authoritative transform synchronization. Required by Networked Actors (the
# presence + replicated-spawn layer the Unreal SDK uses): the server owns each
# object's transform and streams per-client delta snapshots. Enabling this is all
# the server needs — presence is client-driven (your pawn calls AnnouncePresence).
# The client renders remote players interpolated in the past with an adaptive
# jitter buffer that self-tunes to the link (low latency on LAN, robust on WAN).
#
# send_rate_hz drives snapshots (60 = lowest latency; the client sizes its buffer
# from it); sim_hz is the world tick (physics + server_tick). demo_movers /
# player_slots are the built-in moving-cube demos, left OFF so object ids stay
# free for your own Networked-Actor players.
[transport.transform_sync]
enabled = true
send_rate_hz = 60
sim_hz = 60
budget = 0
demo_movers = 0
player_slots = 0
